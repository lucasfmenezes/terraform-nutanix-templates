# VM com disco adicional

Modelo para criar uma VM no Nutanix com um disco extra usando Terraform.

## Uso

~~~bash
cp terraform.tfvars.example terraform.tfvars
nano terraform.tfvars
terraform init
terraform validate
terraform plan
terraform apply
~~~
