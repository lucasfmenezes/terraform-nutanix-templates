# VM Windows Sysprep - modelo original

Este diretório guarda o modelo original em arquivo único, no mesmo formato do exemplo inicial.

## Arquivos

main.tf  
autounattend.xml

## Observação

Este modelo é útil como referência de estudo. Para uso mais organizado, prefira o diretório `nutanix/vm-windows-sysprep`, onde o código foi separado em `provider.tf`, `variables.tf`, `main.tf`, `outputs.tf` e `terraform.tfvars.example`.
